

🧠 Python Quiz App

An interactive, AI-powered quiz application built with Flask, featuring real-time answer explanations generated by OpenAI’s GPT models.
Users can select a programming language, take quizzes, review their history, and receive detailed explanations for each question.

Live Demo: https://pcj-quiz-app.onrender.com

🚀 Features

✅ Dynamic Multiple-Choice Quizzes
	•	Python questions
	•	Randomized order
	•	Tracks score and correctness


🤖 AI-Generated Explanations

After each question, the app uses the OpenAI API to generate an explanation including:

	•	Why the correct answer is right
  
	•	Why other options are wrong
  
	•	Feedback on the user’s selected answer


🎨 Clean, Responsive UI

	•	Uses HTML + CSS templates
  
	•	Smooth progress bar
  
	•	Mobile-friendly layout


⚙️ Kubernetes Self-Healing (Development Mode)

	•	Includes a self-maintenance module that checks for code issues
  
	•	Auto-patch cron job (experimental)
  
	•	Liveness/readiness probes


🏗️ Technologies Used
	•	Python 3 / Flask
  
	•	HTML, CSS, Jinja2
  
	•	OpenAI API
  
	•	Docker (for deployment)
  
	•	Render (hosting)
  
	•	Kubernetes + Minikube (development mode)


📦 Installation (Local Setup)

1. Clone the Repository

git clone https://github.com/Gussyric/python-quiz-app.git
cd python-quiz-app

2. Install Requirements

pip install -r requirements.txt

3. Set Your OpenAI API Key

Create an environment variable:

export OPENAI_API_KEY="your_api_key_here"

4. Run the App

python app.py

Visit:
➡️ http://127.0.0.1:5001


🐳 Docker Setup (Optional)

Build the image:

docker build -t python-quiz-app .

Run the container:

docker run -p 5001:5001 -e OPENAI_API_KEY=$OPENAI_API_KEY python-quiz-app


🌐 Deployment (Render)

This repository is fully configured for Render deployment.

The app runs using:

	•	Dockerfile
  
	•	requirements.txt
  
	•	OPENAI_API_KEY stored in Render environment variables


Live Deployment:
➡️ https://pcj-quiz-app.onrender.com


📁 Project Structure


├── app.py                     # Main Flask application

├── templates/                 # HTML templates (Jinja2)

├── static/                    # CSS, JS, images

├── questions/                 # JSON quiz questions

├── requirements.txt           # Python dependencies

├── Dockerfile                 # Deployment container

├── k8s/                       # Kubernetes config (optional)

└── README.md                  # Project documentation


🧪 Example API Flow

User selects an answer → app calls /quiz/<lang>/answer → AI explanation is generated → user receives feedback.


📌 Notes
	•	No API keys are stored in the repository
  
	•	.gitignore protects all secrets
  
	•	Self-patch Kubernetes tools are for development and not used in Render


🙌 Authors

Omario Guscott

Mark Wells

